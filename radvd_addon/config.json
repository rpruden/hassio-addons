{
  "name": "RADVD Server",
  "version": "1.0",
  "slug": "radvd_addon",
  "description": "Emit IPv6 Router Advertisements.",
  "startup": "services",
  "boot": "auto",
  "host_network": true,
  "arch": ["amd64", "armv7", "armhf", "aarch64"],
  "options": {
    "prefix": "fd00::/64",
    "interface": "eno1",
    "log_level": "error"
  },
  "schema": {
    "prefix": "str",
    "interface": "str",
    "log_level": "list(none|error|warning|info|debug)"
  }
}
